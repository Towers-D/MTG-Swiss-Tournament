import { Coder } from "./Coder";
import { Path } from "./utils/path";
export interface DecodeOptions {
    externalReferences?: Record<string, unknown>;
}
export declare class DecodeContext {
    readonly coder: Coder;
    readonly options?: DecodeOptions | undefined;
    resolvedRefs: Map<number, object>;
    pendingReferences: Map<number, Set<string>>;
    registerPendingReference(id: number, path: Path): void;
    /**
     * Some object needed by some alias (list prepared before) is ready to be used.
     */
    registerRef(id: number, object: object): void;
    resolveRefId(id: number): object | null;
    private readonly warnedOnce;
    warnOnce(key: string, message: string): void;
    readonly externalReferencesMap: Map<string, unknown>;
    constructor(coder: Coder, options?: DecodeOptions | undefined);
}
